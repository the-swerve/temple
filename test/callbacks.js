var Emitter = require('emitter-component');
var assert = require('assert');
var domify = require('domify');
var deja = require('../');

suite('html');

describe('callbacks', function () {

	it('runs a function before the model is rendered');
	it('runs a function after the model is rendered');
	it('runs a function before a specific property is rendered');
	it('runs a function after a specific property is rendered');

});
